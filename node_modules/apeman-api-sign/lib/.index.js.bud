/**
 * Bud file for index.js
 */

'use strict'

const pkg = require('../package.json')
const { Templates } = require('apeman-asset-templates')

module.exports = {
  force: true,
  mode: '444',
  tmpl: Templates.apiIndex,
  data: {
    pkg
  }
}

if (!module.parent) {
  require('coz').render(__filename)
}
