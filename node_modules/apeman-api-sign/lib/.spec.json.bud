/**
 * Bud file for spec
 */
'use strict'

const { fromFile } = require('rfunc-desc')

module.exports = {
  force: true,
  mode: '444',
  data: fromFile(require.resolve('./define')),
  tmpl: (data) => JSON.stringify(data, null, 2)
}

if (!module.parent) {
  require('coz').render(__filename)
}