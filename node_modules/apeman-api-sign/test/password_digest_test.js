/**
 * Test case for passwordDigest.
 * Runs with mocha.
 */
'use strict'

const passwordDigest = require('../lib/digesting/password_digest.js')
const assert = require('assert')
const co = require('co')

describe('password-digests', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Password digests', () => co(function * () {
    let digest = passwordDigest()
    assert.ok(digest)
  }))
})

/* global describe, before, after, it */
