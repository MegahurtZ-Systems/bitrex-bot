/**
 * {{pkg.description}}
 * @module {{ pkg.name }}
 * @version {{ pkg.version }}
 */

'use strict'

const pkg = require('../package.json')
const print = require('./print')
const apemanschema = require('./apemanschema')

let lib = apemanschema.bind(this)
Object.assign(lib, {
  apemanschema,
  print,
  version: pkg.version
})

module.exports = lib
