/**
 * Test case for print.
 * Runs with mocha.
 */
'use strict'

const apemanschema = require('../lib/apemanschema.js')
const assert = require('assert')
const print = require('../lib/print.js')

describe('print', () => {
  it('Print', (done) => {
    let schemas = apemanschema(__dirname + '/../misc/mocks/mock-schemas')
    assert.ok(schemas)
    print(schemas)
    done()
  })
})

/* global describe, it */